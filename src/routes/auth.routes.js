import { Router } from "express";
//import {Registro, signIn} from '../controllers/auth.controller.js'

const router = Router()

//router.post('/registro', Registro)
//router.post('/signup', signIn)

export default router;